#include <stdio.h>



int main()
{
    int n;
    scanf("%d",&n);
    if(n%400==0 || n%4==0)
    
        printf(" %d leap year ",n);
        else if(n%100==0)
        printf(" %d not leap year",n );
    
    else
    printf(" %d not leap year",n );
    
    
    



    return 0;
}
