#include <stdio.h>

int main()
{
    char str[100],ch;
    int i,l;
    printf("enter a string ");
    gets(str);
    for(l=0;str[l];l++);
    
    for(i=0;i<l/2;i++)
    {
    ch=s[i];
    str[i]=str(l-1-i);
    str(l-1-i)=ch;
    }
    printf("reverse %s ",str);
    
    
    return 0;
}
